version: '3.8'

services:
  biomarker:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./outputs:/app/outputs
      - ./reports:/app/reports
      - ./data:/app/data
      - ./env.example:/app/env.example:ro
    env_file:
      - env.example
    restart: unless-stopped
